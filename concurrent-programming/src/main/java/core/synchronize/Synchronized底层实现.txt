TODO. Monitor对象监视器
在Monitor中包含了一些被保护的数据, 每次只有一个线程能访问被保护的数据
1. Synchronization is implemented by using monitors
2. Each object in Java is associated with a monitor, which a thread can lock or unlock
3. Only one thread at a time may hold a lock on a monitor

1. 每个对象除具有关联的monitor监视器外，还具有关联的"wait set"等待集(等待线程的集合)
2. wait set初始时为空,可添加或者删除等待的线程
3. wait set集的操作会被线程的isInterrupted()状态所影响

TODO: synchronized底层实现
对应到操作系统内核线程之上，造成用户态和核心态的转换
绝大部分的并发控制都能够使用synchronized来实现，符合3种特性

1. synchronized的底层实现是完全依赖JVM虚拟机
2. synchronized会由Java虚拟机来保证在出现异常时，锁能够被自动释放
3. 使用synchronized加锁的同步代码块被编译成字节码后，会在同步块前后添加monitor进入和退出的指令
   // 3 monitorenter ---> JVM执行到此处，方法中会调用锁升级的方法
   //                     调用底层C++代码的实现 InterpreterRuntime::monitorenter(..)
   //                     底层使用C++ ObjectMonitor类型，拥有两个等待队列
   // 4 ............
   // 5 ............
   // 10 monitorexit ---> 通过底层代码的实现来释放掉锁
