TODO. CopyOnWrite: 读写分离思想 => 适用于“读多写少”场景
解决高并发读写的问题，保证数据的实时一致性(存在感知的时间差)
1. 在"写线程"写的(几步操作)过程中，"读线程"可能读取到半初始化的数据(脏数据)
2. 为了防止读写并发冲突，CopyOnWrite在操作时将原内存结构Copy一份副本，操作完成之后再替换旧副本
3. 在这个过程中运行存储替换的感知时间差，同时支撑了写的并发操作